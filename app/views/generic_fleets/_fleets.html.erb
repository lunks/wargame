<%= fleet.quantity %>
<%= fleet.generic_unit.name %>
<span style=font-weight:bold><%= "moving to #{fleet.destination.name} " if fleet.moving?%></span>
<%= render :partial => 'move_fleets', :locals => { :fleet => fleet } %>

