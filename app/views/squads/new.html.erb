New Squad

<%= simple_form_for @squad do |f| %>
<%= f.input :name %>
<%= f.input :color, :collection => @colors  %>
<%= f.input  :faction, :collection => FACTIONS %>
<%= f.button :submit %>
<% end %>

